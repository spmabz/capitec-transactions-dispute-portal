services:
  portal:
    build:
      context: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      DB_PATH: /app/data/disputes.db
    volumes:
      - dispute-portal-data:/app/data
    restart: unless-stopped

volumes:
  dispute-portal-data:
